# Compiled binary (built with `go build -o iso8583lens ./cmd/iso8583lens`)
/iso8583lens

# Go workspace / test artifacts
*.test
*.out

# Local secrets — never commit. Copy .env.example to .env and fill in your keys.
.env
